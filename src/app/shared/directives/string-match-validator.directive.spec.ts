import { StringMatchValidatorDirective } from './string-match-validator.directive';

describe('StringMatchValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new StringMatchValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
