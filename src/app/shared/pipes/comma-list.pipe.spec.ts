import { CommaListPipe } from './comma-list.pipe';

describe('CommaListPipe', () => {
  it('create an instance', () => {
    const pipe = new CommaListPipe();
    expect(pipe).toBeTruthy();
  });
});
