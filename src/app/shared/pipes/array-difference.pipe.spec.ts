import { ArrayDifferencePipe } from './array-difference.pipe';

describe('ArrayDifferencePipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayDifferencePipe();
    expect(pipe).toBeTruthy();
  });
});
